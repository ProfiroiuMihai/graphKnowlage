##Install virtualenv if not exist
##Golbally added virtualenv
pip install virtualenv

##Create Local Enviromnemt
virtualenv env
source env/bin/activate
pip install -r requirements.txt
streamlit run main.py